# Testing and validation script dependencies
# For deployment validation and smoke testing

# HTTP requests and async support
requests>=2.31.0
aiohttp>=3.8.0

# YAML configuration parsing  
PyYAML>=6.0

# JSON processing (built-in, but explicit)
# json - built-in module

# Azure SDK components for testing
azure-functions>=1.18.0
azure-cosmos>=4.5.0
azure-keyvault-secrets>=4.7.0
azure-identity>=1.15.0
azure-monitor-opentelemetry>=1.1.0

# OpenCensus for Application Insights testing
opencensus-ext-azure>=1.1.0

# Async support
asyncio  # built-in module

# Development and testing utilities
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: Progress bars and better CLI output
tqdm>=4.66.0
