# Vimarsh Backend - Optimized Requirements for CI/CD
# This file contains minimal dependencies for faster installation in CI/CD

# Azure Functions Core (Essential)
azure-functions==1.18.0

# Essential Python packages
setuptools>=65.0.0
wheel>=0.40.0

# Azure Services (Core only)
azure-cosmos==4.5.1
azure-storage-blob==12.19.0
azure-identity==1.15.0
azure-keyvault-secrets==4.7.0

# Authentication & Security (Minimal)
PyJWT[crypto]==2.8.0
cryptography==41.0.7

# AI/ML Libraries (Essential only)
google-generativeai==0.7.2
numpy>=1.21.0,<2.0.0  # Pin to avoid conflicts

# FastAPI/HTTP (Lightweight)
fastapi>=0.104.0,<0.105.0
uvicorn[standard]==0.24.0
pydantic>=2.0.0,<3.0.0

# Database & Storage (Essential)
pymongo>=4.3.0,<5.0.0
python-dotenv>=1.0.0

# Utilities (Minimal)
python-dateutil>=2.8.0
requests>=2.31.0

# Monitoring (Basic)
azure-monitor-opentelemetry==1.2.0

# Development & Testing (CI/CD optimized)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Note: Heavy dependencies moved to requirements-full.txt
# For local development, use: pip install -r requirements-full.txt
# For CI/CD, this file provides faster installation
